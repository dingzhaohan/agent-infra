# 任务
给定一个开源的github仓库地址，或者也会同时给出对应的 docs地址。
我们希望能够部署这个github对应的工具。
部署的方式，考虑使用dockerfile。所以问题的前一步，我们需要转换成如何生成稳定可运行的dockerfile。
这个任务是为部署开源科学工具服务的，需要有llm生成对应的工具的最佳部署指南（dockerfile），工具相关的description
这必然意味着中间需要多步的探寻代码库，解析这个工具，对于有readme的，需要能够精准命中这个关键文件，来进行处理。
理想情况下使用了readme后，
建立能够理想上搞定的，以及可以通过其他再多一步的方式可能能够搞定的，以及目前无法正确实现部署的（用于后续bad case）
建立一个基于agno框架的利用其中workflow的编排工作流，中间会借助大语言模型能力去对批量的开源的github仓库地址（开源工具池子，由人工整理好需要探索的工具列表），结合软件工程专家定制化专业的思路去解析一个开源仓库，结合其中可能存在问题去搜寻文档或者仓库中可以提示的内容，来完成dockerfile的构建以及运行，同时最后还需要verify，直到verify已经成功启动这个工具。

使用git clone 到本地 然后进行搜索，这个agent需要能够terminal操作。在自己的workdir下工作。
注意到这个agent的所有的操作都放在./repos工作目录下，对于每一个工具，clone到这个repos下的子目录进行操作，使得仓库之间能够隔离。

这个agent基于agno，希望能够使用terminal tools，能够使用docker tools，还有能够比较高级反思，还有错误反馈和纠正的，以及最后有verify的逻辑，确保对于每个repo仓库能够正确结项。

# 具体步骤

有无明确的dockerfile，可能不只是在项目根目录
如果有，直接使用作为镜像构建。也可能直接在官方中能够直接拉取镜像。
如果无，如果是readme中包含了依赖安装，并且项目根目录中已经有了requirements.txt，或者其他依赖安装工具，可能是javascript或者rust或者其他语言，按照对应的安装和运行方式来去进行。
如果这些都没有，并且没有好的方式，包括可能潜在的cicd或者凡是能够有助于运行启动这个工具的



# 需要攻克的开源仓库以及对应的仓库地址和潜在的文档地址
放在./list.md中

